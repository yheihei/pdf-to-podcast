[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
google-genai = ">=1.0.0"
"pdfminer.six" = ">=20221105"
pypdf = ">=3.17.0"
pytest = ">=7.4.0"
pytest-cov = ">=4.1.0"
pytest-mock = ">=3.12.0"
python-dotenv = ">=1.0.0"
pydub = ">=0.25.0"
mutagen = ">=1.47.0"
rich = ">=13.7.0"
tqdm = ">=4.66.0"
librosa = ">=0.10.0"
soundfile = ">=0.12.0"
numpy = ">=1.24.0"

[dev-packages]

[requires]
python_version = "3.11"
